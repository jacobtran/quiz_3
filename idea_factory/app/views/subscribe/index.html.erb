<div class="container">
  <h1>Subscirbe to Our Newsletter</h1>

<%# form_tag is a built-in Rails view helper that enables us to generate a form
    the first argument is the `action` attribute of the form. You can provide
    method: attribute to make the form use any type of request including: GET
    POST, PATCH and DELETE %>
<%= form_tag subscribe_path, method: :post do %>
  <div>
    <%= label_tag :name %>
    <%= text_field_tag :name, nil, class:"form-control" %>
  </div>
  <div>
    <%= label_tag :email %>
    <%= email_field_tag :email, nil, class:"form-control" %>
  </div>
  <%= submit_tag "Subscribe Now!", class:"btn btn-default" %>
<% end %>

</div>
